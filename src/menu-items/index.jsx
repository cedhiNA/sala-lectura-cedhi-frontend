// project-imports
import dashboard from './dashboard';
import library from './library';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, library]
};

export default menuItems;
